#
# Author : Gaston Gonzalez!/bin/bash
#
# Author : Gaston Gonzalez
#
# Author : Gaston Gonzalez Date   : 25 October 2024
#
# Author : Gaston Gonzalez Updated: 4 November 2024 
#
# Author : Gaston Gonzalez Purpose: Set user-specific settings for LiaisonOS.
#
# Author : Gaston Gonzalez
#
# Author : Gaston Gonzalez Modified by: Sylvain Deguire - VA2OPS
#
# Author : Gaston Gonzalez Date       : 16 November 2025
#
# Author : Gaston Gonzalez Changes    : Added bilingual French/English support and password security improvements
#
# Author : Gaston Gonzalez
#
# Author : Gaston Gonzalez Modified by: Sylvain Deguire - VA2OPS
#
# Author : Gaston Gonzalez Date       : 1 December 2025
#
# Author : Gaston Gonzalez Changes    : Added PostgreSQL user creation with superuser rights
#
# Author : Gaston Gonzalez
#
# Author : Gaston Gonzalez Modified by: Sylvain Deguire - VA2OPS
#
# Author : Gaston Gonzalez Date       : January 2026
#
# Author : Gaston Gonzalez Changes    : Fixed password status display after user input, fixed NOPASS detection
#
# Author : Gaston Gonzalez
. /opt/emcomm-tools/bin/et-common

USER_CONF=~/.config/emcomm-tools/user.json

#
# Author : Gaston Gonzalez-----------------------------------------------------------
#
# Author : Gaston Gonzalez Function: create_postgres_user
#
# Author : Gaston Gonzalez Purpose : Create or update PostgreSQL user with superuser rights
#
# Author : Gaston Gonzalez           Uses callsign as username and Winlink password
#
# Author : Gaston Gonzalez-----------------------------------------------------------
create_postgres_user() {
    local callsign="${1,,}"  #
# Author : Gaston Gonzalez lowercase for PostgreSQL username
    local password="$2"
    local lang="$3"
    
    #
# Author : Gaston Gonzalez Skip if PostgreSQL is not installed or not running
    if ! command -v psql &> /dev/null; then
        return 0
    fi
    
    if ! systemctl is-active --quiet postgresql; then
        if [ "$lang" == "fr" ]; then
            echo "Note: PostgreSQL n'est pas en cours d'exécution. L'utilisateur sera créé au prochain démarrage."
        else
            echo "Note: PostgreSQL is not running. User will be created on next startup."
        fi
        return 0
    fi
    
    #
# Author : Gaston Gonzalez Validate inputs
    if [ -z "$callsign" ] || [ "$callsign" == "null" ]; then
        return 0
    fi
    
    if [ -z "$password" ] || [ "$password" == "null" ]; then
        if [ "$lang" == "fr" ]; then
            echo "Note: Mot de passe requis pour créer l'utilisateur PostgreSQL."
        else
            echo "Note: Password required to create PostgreSQL user."
        fi
        return 0
    fi
    
    #
# Author : Gaston Gonzalez Create or update PostgreSQL user with superuser rights
    sudo -u postgres psql -q -c "DO \$\$
    BEGIN
        IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname = '${callsign}') THEN
            CREATE USER ${callsign} WITH PASSWORD '${password}' SUPERUSER CREATEDB CREATEROLE;
        ELSE
            ALTER USER ${callsign} WITH PASSWORD '${password}' SUPERUSER CREATEDB CREATEROLE;
        END IF;
    END \$\$;" 2>/dev/null
    
    #
# Author : Gaston Gonzalez Create user's personal database if not exists
    sudo -u postgres psql -q -tc "SELECT 1 FROM pg_database WHERE datname = '${callsign}'" 2>/dev/null | grep -q 1 || \
        sudo -u postgres createdb -O "${callsign}" "${callsign}" 2>/dev/null
    
    #
# Author : Gaston Gonzalez Grant access to shared emcomm database if it exists
    sudo -u postgres psql -q -tc "SELECT 1 FROM pg_database WHERE datname = 'emcomm'" 2>/dev/null | grep -q 1 && \
        sudo -u postgres psql -q -c "GRANT ALL PRIVILEGES ON DATABASE emcomm TO ${callsign};" 2>/dev/null
    
    if [ "$lang" == "fr" ]; then
        echo "Utilisateur PostgreSQL '${callsign}' configuré avec droits superutilisateur."
    else
        echo "PostgreSQL user '${callsign}' configured with superuser rights."
    fi
}

#
# Author : Gaston Gonzalez-----------------------------------------------------------
#
# Author : Gaston Gonzalez Main script
#
# Author : Gaston Gonzalez-----------------------------------------------------------

if [ ! -f ${USER_CONF} ]; then
    mkdir -p $(dirname ${USER_CONF})
    cp /etc/skel/.config/emcomm-tools/user.json ${USER_CONF}
fi

#
# Author : Gaston Gonzalez Read current language (default to English if not set)
LANGUAGE=$(jq -r '.language // "en"' ${USER_CONF})

#
# Author : Gaston Gonzalez STEP 1: Ask for interface language FIRST
echo ""
echo "EmComm menu language / Langue du menu EmComm:"
read -p "(E)nglish or (F)rançais [${LANGUAGE}]: " lang_input
lang_input=${lang_input:-$LANGUAGE}

#
# Author : Gaston Gonzalez Normalize input
case "${lang_input,,}" in
    f|fr|french|français)
        LANGUAGE="fr"
        ;;
    e|en|english|anglais)
        LANGUAGE="en"
        ;;
    *)
        LANGUAGE="en"  #
# Author : Gaston Gonzalez Default to English
        ;;
esac

#
# Author : Gaston Gonzalez Read other current values
CALLSIGN=$(jq -r '.callsign' ${USER_CONF})
GRID=$(jq -r '.grid' ${USER_CONF})
WINLINK_PASSWORD=$(jq -r '.winlinkPasswd' ${USER_CONF})

#
# Author : Gaston Gonzalez Check password status for secure display
if [ "$WINLINK_PASSWORD" == "null" ] || [ "$WINLINK_PASSWORD" == "" ] || [ -z "$WINLINK_PASSWORD" ] || [ "$WINLINK_PASSWORD" == "NOPASS" ]; then
    PASSWORD_STATUS="not set"
    PASSWORD_STATUS_FR="non défini"
else
    PASSWORD_STATUS="***configured***"
    PASSWORD_STATUS_FR="***configuré***"
fi

#
# Author : Gaston Gonzalez STEP 2: Display prompts in selected language
if [ "$LANGUAGE" == "fr" ]; then
    #
# Author : Gaston Gonzalez French prompts
    echo ""
    echo "Configuration actuelle:"
    echo "  Indicatif: ${CALLSIGN}"
    echo "  Grille Maidenhead: ${GRID}"
    echo "  Mot de passe Winlink: ${PASSWORD_STATUS_FR}"
    echo ""

    read -p "Indicatif [${CALLSIGN}]: " input
    CALLSIGN=${input:-$CALLSIGN}

    read -p "Grille Maidenhead [${GRID}]: " input
    GRID=${input:-$GRID}

    read -s -p "Mot de passe Winlink [${PASSWORD_STATUS_FR}]: " input
    WINLINK_PASSWORD=${input:-$WINLINK_PASSWORD}
    echo ""

    #
# Author : Gaston Gonzalez Update password status after user input
    if [ -n "$input" ]; then
        PASSWORD_STATUS_FR="***sera défini***"
    fi

    echo ""
    echo "Nouvelle configuration:"
    echo "  Langue: Français"
    echo "  Indicatif: ${CALLSIGN}"
    echo "  Grille Maidenhead: ${GRID}"
    echo "  Mot de passe Winlink: ${PASSWORD_STATUS_FR}"
    echo ""

    read -p "Sauvegarder la configuration? (o/n): " confirm
    confirm_yes="o"
    saved_msg="Configuration sauvegardée."
    not_saved_msg="Configuration non sauvegardée."
else
    #
# Author : Gaston Gonzalez English prompts
    echo ""
    echo "Current configuration:"
    echo "  Callsign: ${CALLSIGN}"
    echo "  Maidenhead Grid: ${GRID}"
    echo "  Winlink Password: ${PASSWORD_STATUS}"
    echo ""

    read -p "Callsign [${CALLSIGN}]: " input
    CALLSIGN=${input:-$CALLSIGN}

    read -p "Maidenhead Grid [${GRID}]: " input
    GRID=${input:-$GRID}

    read -s -p "Winlink Password [${PASSWORD_STATUS}]: " input
    WINLINK_PASSWORD=${input:-$WINLINK_PASSWORD}
    echo ""

    #
# Author : Gaston Gonzalez Update password status after user input
    if [ -n "$input" ]; then
        PASSWORD_STATUS="***will be set***"
    fi

    echo ""
    echo "New configuration:"
    echo "  Language: English"
    echo "  Callsign: ${CALLSIGN}"
    echo "  Maidenhead Grid: ${GRID}"
    echo "  Winlink Password: ${PASSWORD_STATUS}"
    echo ""

    read -p "Save configuration? (y/n): " confirm
    confirm_yes="y"
    saved_msg="Configuration saved."
    not_saved_msg="Configuration not saved."
fi

#
# Author : Gaston Gonzalez STEP 3: Save with language field included
if [ "$confirm" == "$confirm_yes" ]; then
    jq --arg language "$LANGUAGE" \
       --arg callsign "$CALLSIGN" \
       --arg grid "$GRID" \
       --arg winlinkPasswd "$WINLINK_PASSWORD" \
       '.language = $language | .callsign = $callsign | .grid = $grid | .winlinkPasswd = $winlinkPasswd' \
       ${USER_CONF} > ${USER_CONF}.tmp && mv ${USER_CONF}.tmp ${USER_CONF}
    echo "$saved_msg"
    
    #
# Author : Gaston Gonzalez Create/update PostgreSQL user
    create_postgres_user "$CALLSIGN" "$WINLINK_PASSWORD" "$LANGUAGE"
else
    echo "$not_saved_msg"
fi
